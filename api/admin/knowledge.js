const {requireAdmin}=require('../_auth');
const OWNER='limagourmet9-prog',REPO='ATARAXIA.SENS.github.io',PATH='frontend/public/ai-knowledge.json';
async function gh(path,opts={}){const r=await fetch(`https://api.github.com/repos/${OWNER}/${REPO}/contents/${path}`,{...opts,headers:{Accept:'application/vnd.github+json',Authorization:`Bearer ${process.env.GITHUB_CONTENTS_TOKEN}`,'X-GitHub-Api-Version':'2022-11-28','Content-Type':'application/json'}});if(!r.ok)throw new Error(await r.text());return r.json()}
module.exports=async function(req,res){if(!requireAdmin(req,res))return;if(!process.env.GITHUB_CONTENTS_TOKEN)return res.status(503).json({error:'Repositorio no configurado'});try{const file=await gh(PATH);if(req.method==='GET'){return res.json(JSON.parse(Buffer.from(file.content,'base64').toString('utf8')))}if(req.method==='PUT'){const data=req.body;if(!data||typeof data!=='object'||!data.version||!data.services)return res.status(400).json({error:'Estructura de conocimiento inválida'});const content=Buffer.from(JSON.stringify(data,null,2)+'\n').toString('base64');const result=await gh(PATH,{method:'PUT',body:JSON.stringify({message:`Update ATARAXIA AI knowledge ${data.version}`,content,sha:file.sha,branch:'main'})});return res.json({ok:true,commit:result.commit?.sha})}return res.status(405).json({error:'Método no permitido'})}catch(e){return res.status(500).json({error:'No se pudo acceder al conocimiento'})}};
